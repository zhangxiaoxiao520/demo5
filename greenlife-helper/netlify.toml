[build]
  command = "npm run build"
  publish = ".next"
  
[build.environment]
  NODE_VERSION = "20"

# Netlify Next.js Runtime settings
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Simple redirect rule for Next.js SPA fallback
[[redirects]]
  from = "/*"
  to = "/"
  status = 200